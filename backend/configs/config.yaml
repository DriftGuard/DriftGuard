server:
  port: 8080
  read_timeout: 30s
  write_timeout: 30s
  idle_timeout: 60s
  shutdown_timeout: 30s

database:
  host: localhost
  port: 27017
  dbname: DriftGuard
  user: ""

kubernetes:
  config_path: "/Users/arpitsrivastava/.kube/config"
  context: ""
  namespaces: ["driftguard"]
  resources: [
    "deployments",     # Application deployments
    "services",        # Network services
    "configmaps",      # Configuration data
    "secrets"          # Sensitive data
    # "pods",          # Commented out - too frequent changes
    # "ingresses",     # Commented out - not needed
    # "persistentvolumeclaims", # Commented out - storage
    # "serviceaccounts",       # Commented out - RBAC
    # "networkpolicies"        # Commented out - networking
  ]
  labels: {}
  # enable_snapshots: true
  # snapshot_interval: 5m
  # skip_system_namespaces: true
  # skip_frequent_pods: true

git:
  default_branch: main
  clone_timeout: 5m
  pull_timeout: 2m

# mcp:
#   endpoint: localhost:9000
#   timeout: 30s
#   retries: 3

environments:
  - name: development
    description: Development environment
    labels:
      env: dev
    k8s_context: dev-cluster

logging:
  level: info
  format: json
  output_path: "" 